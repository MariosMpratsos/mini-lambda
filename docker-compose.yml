services:
  mini-lambda:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - .:/app
      - /app/node_modules
    environment:
      - HOST_PROJECT_PATH=${PWD}
